/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "br.pinterest.com",
      "images.unsplash.com",
      "i.pinimg.com",
    ],
  },
};

export default nextConfig;
